package com.leanderson.jpa.repository;

import com.leanderson.jpa.Perfil;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PerfilRepository extends JpaRepository<Perfil, Long> {
}
